name: Invalid - Unpinned Actions

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # This uses a tag reference instead of SHA - violation
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24'
      - run: go test ./...
